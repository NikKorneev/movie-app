import { View, Text } from "react-native";
import React from "react";

const Divider = () => {
	return (
		<View className="bg-neutral-400 h-[100%] w-[2px] rounded-full"></View>
	);
};

export default Divider;
